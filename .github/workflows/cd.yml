name: cd
on:
  workflow_dispatch:
    inputs:
      env:
        description: Environment (dev|test|prod)
        required: true
        default: dev
  push:
    branches:
    - main
    paths:
    - source
jobs:
  continuous-delivery:
    runs-on: ubuntu-latest
    env:
      ENV_PREFIX: "${{ github.event.inputs.env == '' && 'dev' || github.event.inputs.env }}"
    steps:
    - uses: actions/checkout@v3
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_SCM_CREDS }}
    - name: Upgrade bicep
      run: az bicep upgrade
    - uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.0.x
    - name: Publish app
      run: |
        dotnet publish ./source/ExtractorFunc -c Release -o '${{ github.workspace }}/published'